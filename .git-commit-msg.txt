feat: inline "report extract" button on every non-chart module

Adds a small Excel/PDF export button next to the live filters on every
data/table-driven analytics page (as opposed to /reports, which requires
navigating away and re-picking a separate copy of the filters). It reuses
the exact same REPORT_DEFINITIONS the /reports catalog already has, just
reading period/principal straight off the live global store — what
downloads always matches what's already on screen, with zero extra steps.

Placement: GlobalFilterBar (shared by every analytics page except
Timestamps, which has its own control bar) resolves the current page's
report via pageKeyForPathname and renders InlineReportExport there.
Financials is the one page with two reports behind one URL (Profitability
and Receivables, tab-selected), so it gets its own tab-aware placement
instead of going through GlobalFilterBar.

Also adds the five report definitions that didn't exist yet - Order 360,
Sales & Returns, Dormant OOS, Principal KPIs (Mars), and Receivables (new
GET /api/receivables, wrapping the same getReceivablesDashboard() the
Executive Summary page already calls server-side) - so every module now
has one, matching the other ten already on /reports.

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
